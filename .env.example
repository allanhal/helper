# Meeting Helper — Environment Variables
# Copy this file to .env and fill in values as needed.

# No environment variables are required for local development.
# Ollama must be running at localhost:11434 (default).

# For release/upload scripts only:
# BLOB_READ_WRITE_TOKEN=  # Vercel Blob token (needed for `pnpm release` uploads)
