## What does this PR do?

## Why?

## How to test

## Checklist
- [ ] `pnpm lint` passes
- [ ] `pnpm tsc --noEmit` passes (frontend)
- [ ] `pnpm tsc -p electron/tsconfig.json --noEmit` passes (electron)
- [ ] Tested with at least one Ollama model
- [ ] Screenshots attached (if UI change)
